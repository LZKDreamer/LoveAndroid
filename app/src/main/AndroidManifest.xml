<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lzk.loveandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".Collection.CollectionActivity"></activity>
        <activity android:name=".Knowledge.Activity.KnowledgeListActivity" />
        <activity android:name=".Search.Activity.SearchListActivity" />
        <activity android:name=".Search.Activity.SearchActivity" />
        <activity android:name=".CommonWeb.Activity.CommonWebActivity" />
        <activity android:name=".main.ArticleDetailActivity" />
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".Login.LoginActivity" />

        <meta-data
            android:name="android.notch_support"
            android:value="true" />

        <activity android:name=".main.MainActivity" />
        <activity
            android:name=".main.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>